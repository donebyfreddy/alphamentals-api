{
  "name": "alphamentals-api-server",
  "private": true,
  "description": "AlphaMentals API server — runs on the VPS as the backend for the Vercel frontend.",
  "scripts": {
    "dev": "cd .. && npm run dev:api",
    "build": "cd .. && npm run build:api",
    "start": "cd .. && npm run start:api"
  }
}
